{
  "name": "lazycoder_leptos",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bunx tailwindcss -i style/tailwind.css -o style/main.css --watch",
    "test": "cargo leptos test",
    "test:ssr": "cargo leptos test --bin-features ssr --lib-features hydrate --bin-default-features false --lib-default-features false",
    "serve:ssr": "cargo leptos serve --bin-features ssr --lib-features hydrate --bin-default-features false --lib-default-features false",
    "watch:ssr": "cargo leptos watch --bin-features ssr --lib-features hydrate --bin-default-features false --lib-default-features false"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.11",
    "@tailwindcss/typography": "^0.5.16",
    "@types/bun": "latest",
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.11"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
